import Controller from '@ember/controller';

export default Controller.extend({
  actions: {
    // viewBlog(item) {
    //   let id = item.id;
    //   this.transitionToRoute('blogs', { path: 'view/' + id });
    // },
  }
});
